<UserControl x:Class="OpenStartScreen.Tile"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="124" Height="124">
    <Border BorderBrush="Transparent" BorderThickness="4px" Background="SkyBlue">
        <Grid>
            <Image Source="{Binding TileImage, RelativeSource={RelativeSource AncestorType=UserControl}}" Width="64" Height="64" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding TileName, RelativeSource={RelativeSource AncestorType=UserControl}}" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="10,0,0,10"/>
        </Grid>
    </Border>
</UserControl>
